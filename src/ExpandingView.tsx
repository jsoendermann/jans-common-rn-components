import { ViewProperties } from 'react-native'
import { StyledComponentClass } from 'styled-components'
import styled from 'styled-components/native'

export const ExpandingView = styled.View`flex: 1;`
