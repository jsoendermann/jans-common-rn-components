import { ViewProperties } from 'react-native'
import { StyledComponentClass } from 'styled-components'
import styled from 'styled-components/native'

export const HorizontalView = styled.View`flex-direction: row;`
